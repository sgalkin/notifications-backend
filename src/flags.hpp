#pragma once

#include <gflags/gflags_declare.h>

//DECLARE_string(host);
//DECLARE_int32(port);

DECLARE_string(apns_cert);
DECLARE_string(apns_key);

DECLARE_string(https_cert);
DECLARE_string(https_key);
DECLARE_int32(https_port);
